{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "command": "npx", // Could be any other shell command
            "args": ["tsc", "--project", "./tsconfig.electron.json"]
        },
        {
            "label": "terminate-vite",
            "type": "process",
            "command": "npx",
            "args": ["kill-port", "5173"]
        }
    ]
}
